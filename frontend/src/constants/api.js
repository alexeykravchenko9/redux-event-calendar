export const API_GENERAL = `http://localhost:${SERVER_PORT}`;
export const API_EVENTS = `http://localhost:${SERVER_PORT}/api/events`;
export const API_LOGIN_URL = `http://localhost:${SERVER_PORT}/api/login`;
export const API_LOGOUT_URL = `http://localhost:${SERVER_PORT}/api/logout`;